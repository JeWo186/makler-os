/** Single source of truth for layout tokens.
 *  Import these instead of writing Tailwind classes inline. */

/** Standard page container — max width + consistent horizontal padding */
export const container = 'max-w-7xl mx-auto px-6 lg:px-8'

/** Default vertical padding for content sections (PageSection default) */
export const sectionPadding = 'py-20'

/** Tighter vertical padding for dense listing/form areas */
export const contentPadding = 'py-10'
